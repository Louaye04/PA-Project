# Example env file for backend
# Copy to .env and fill real values (DO NOT commit .env with secrets)

# SMTP settings for nodemailer (Gmail example)
# If you use Gmail, generate an "App Password" (requires 2FA) and use it as EMAIL_PASS
EMAIL_USER=hindkahla2@gmail.com
EMAIL_PASS=YOUR_APP_PASSWORD_HERE

# Optional bind port/host
PORT=5000
BIND_HOST=0.0.0.0

# OTP secret (change in production)
OTP_SECRET=change-me-to-a-random-secret
